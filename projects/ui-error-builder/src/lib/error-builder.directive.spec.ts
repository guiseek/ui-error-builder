import { ErrorBuilderDirective } from './error-builder.directive';

describe('ErrorBuilderDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorBuilderDirective();
    expect(directive).toBeTruthy();
  });
});
